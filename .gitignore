node_modules/
dist/
build/
out/
coverage/
.turbo/

# Anything a running instance writes: databases, backups, published proofs,
# the publisher's cursor. None of it is source, and some of it is personal.
*.db
*.db-journal
*.db-wal
*.db-shm
.data/
data/

# Key material and configuration never belong in this repository. .env.example
# is the documented shape; the real file stays on the machine that runs it.
.env
.env.*
!.env.example
*.key
*.pem
id_ed25519
id_rsa
keys/

# Generated by the toolchain
packages/core/generated/
packages/contracts/artifacts/
packages/contracts/cache/
packages/contracts/typechain-types/
*.tsbuildinfo

# Packaged tarballs and archives
*.tgz
*.tar.gz

*.log
.DS_Store
Thumbs.db
